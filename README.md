# Keeto

Zero-config AI observability for Python.

```python
from keeto import monitor

monitor.start()

# Your existing AI code — unchanged
client.chat.completions.create(...)
```

See [PLAN.md](PLAN.md) for the full architecture and roadmap.
